# Design Context — Clawd Desktop Pet

> Visual language for Clawd, a desktop AI pet agent built with Electron.
> Transparent overlay + standard windows for chat, settings, and quick input.

---

## Users

Tech-savvy individuals who enjoy AI tools. They value:
- **Clarity over decoration** — they want to see what's happening, not be sold to
- **Keyboard and workflow efficiency** — they use tools daily, not once
- **Transparency** — they want to understand what the system is doing
- **Tasteful defaults** — they appreciate good design but don't want to configure it

Context of use: personal productivity, creative work, technical exploration. Often running alongside editors, terminals, and browsers — the UI must feel at home in a dense workspace.

---

## Brand Personality

**Three words:** Precise. Warm. Forward.

| Dimension | Expression |
|-----------|-----------|
| Precise | Every pixel has intent. No decorative noise. Information density without clutter. |
| Warm | Soft edges, comfortable spacing, approachable type. Never cold or clinical. |
| Forward | Subtle glass effects, refined motion, modern color usage. Feels like the near future, not sci-fi. |

**What this is NOT:**
- NOT enterprise OA — no dense toolbars, no status bar overload
- NOT flashy SaaS — no gradient salads, no 3D illustrations, no hero sections

---

## Design Principles

1. **Quiet confidence** — The interface doesn't shout. It works. Whitespace is a feature, not waste. Let content breathe.

2. **Glass, not chrome** — Use translucency and depth (backdrop-filter, soft shadows) to create hierarchy. Never use heavy borders, raised bevels, or strong gradients to separate layers.

3. **Round but not bubbly** — Generous border-radius (12-16px on surfaces) creates warmth without looking toy-like. Buttons at 14px feel clickable, not childish.

4. **Motion as meaning** — 150ms transitions for state changes. Animations communicate (loading, success, error), never decorate. If reduced-motion is active, everything still works.

5. **Dark-first** — Dark theme is the primary and only theme. Design every token against dark backgrounds. Use Slate-tinted darks, never pure black.

---

## Visual Language

### Color System

#### Dark-Only Token System

Use Slate-tinted darks as the foundation. All values below are for dark mode.

| Role | Token | Value |
|------|-------|-------|
| Page background | `--bg-page` | `#0f172a` |
| Card/surface | `--bg-card` | `#1e293b` |
| Elevated surface | `--bg-elevated` | `rgba(30,41,59,0.88)` with blur |
| Header bar | `--bg-header` | `#25262b` |
| Sidebar | `--bg-sidebar` | `#1e1e26` |
| Input background | `--bg-input` | `#2a2c34` |
| Border | `--border` | `rgba(255,255,255,0.10)` |
| Border subtle | `--border-subtle` | `rgba(255,255,255,0.06)` |
| Text primary | `--text-primary` | `rgba(241,245,249,0.95)` |
| Text secondary | `--text-secondary` | `rgba(148,163,184,0.80)` |
| Text tertiary | `--text-tertiary` | `rgba(100,116,139,0.70)` |

#### Brand Color

General brand: **`#2563eb`** (blue-600). Used for generic UI elements, links, default accents.

| Token | Value | Usage |
|-------|-------|-------|
| `--brand` | `#2563eb` | Buttons, links, active states |
| `--brand-hover` | `#1d4ed8` | Hover states |
| `--brand-light` | `#60a5fa` | Brand in dark context, lighter variant |
| `--brand-glow` | `rgba(37,99,235,0.15)` | Focus rings, subtle highlights |

#### Pet Role Colors

Each pet profile has a distinct role color for identity. Used for pet name highlights, status indicators, chat bubble accents, and profile cards.

| Role | Token | Value |
|------|-------|-------|
| Chief | `--role-chief` | `#e8912d` |
| Coder | `--role-coder` | `#4a90d9` |
| Scout | `--role-scout` | `#50b478` |
| Analyst | `--role-analyst` | `#9b6dd7` |
| Custom | `--role-custom` | `#888888` |

#### Semantic Colors

| Role | Token | Value |
|------|-------|-------|
| Success | `--success` | `#34d399` |
| Success bg | `--success-bg` | `rgba(16,185,129,0.15)` |
| Warning | `--warning` | `#fbbf24` |
| Warning bg | `--warning-bg` | `rgba(245,158,11,0.15)` |
| Danger | `--danger` | `#f87171` |
| Danger bg | `--danger-bg` | `rgba(239,68,68,0.15)` |

#### Navigation States

| State | Value |
|-------|-------|
| Hover | `rgba(96,165,250,0.10)` |
| Active | `rgba(96,165,250,0.15)` |
| Active border | `rgba(96,165,250,0.30)` |

---

### Typography

#### Font Stacks

```css
--font-body: "Segoe UI", -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
--font-mono: "Cascadia Code", "Fira Code", "Consolas", monospace;
```

#### Type Scale

| Role | Size | Weight | Letter-spacing | Line-height |
|------|------|--------|---------------|-------------|
| Page title | 28px | 700 | 0.5px | 1.2 |
| Section title | 22px | 700 | — | 1.2 |
| Card title | 15px | 700 | 0.1px | 1.3 |
| Body | 14px | 400 | 0.2px | 1.5 |
| Label | 12px | 600 | — | 1.4 |
| Caption/help | 11px | 400-500 | — | 1.4 |
| Code | 13px | 400 | — | 1.6 |

Set `-webkit-font-smoothing: antialiased`.

---

### Spacing

4px base grid.

| Token | Value | Usage |
|-------|-------|-------|
| `--space-1` | 4px | Tight gaps (icon-to-text, inline) |
| `--space-2` | 8px | Standard small gap (form fields, list items) |
| `--space-3` | 12px | Default gap (card margins, section spacing) |
| `--space-4` | 16px | Large padding (panels, sections) |
| `--space-5` | 20px | Page-level spacing |
| `--space-6` | 24px | Modal/dialog padding |
| `--space-8` | 32px | Major sections |

---

### Border Radius

| Element | Token | Value |
|---------|-------|-------|
| Cards, panels | `--radius-card` | 12px |
| Buttons (primary) | `--radius-btn` | 14px |
| Buttons (secondary) | `--radius-btn-sm` | 10px |
| Inputs | `--radius-input` | 8px |
| Modal/dialog | `--radius-modal` | 16px |
| Navigation items | `--radius-nav` | 10px |
| Pills, badges, avatars | `--radius-pill` | 999px |
| Code blocks | `--radius-code` | 8px |

Key principle: **surfaces > 12px, interactive elements 8-14px, decorative pills 999px**.

---

### Shadows

| Level | Value | Usage |
|-------|-------|-------|
| Subtle | `0 1px 2px rgba(0,0,0,0.08)` | Cards resting state |
| Surface | `0 8px 32px rgba(0,0,0,0.16)` | Panels, sidebar |
| Brand glow | `0 4px 24px rgba(37,99,235,0.20)` | Focused/active elements |
| Elevated | `0 24px 64px rgba(0,0,0,0.32)` | Modals, popovers |

Dark mode uses 2-3x higher opacity than light mode equivalents.

---

### Glass Effect

```css
.glass-panel {
  background: rgba(30, 41, 59, 0.88);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.10);
  border-radius: 12px;
}
```

Use for sidebar, chat panel, floating elements. NOT for inline content or form fields.

---

### Motion

| Property | Duration | Easing | Usage |
|----------|----------|--------|-------|
| Color/opacity | 150ms | ease | Hover states, text color changes |
| Transform | 150ms | ease | Icon rotations, scale on hover |
| Layout | 200ms | ease | Sidebar collapse, grid resize |
| Slide transitions | 250ms | ease-out | Chat panel slide-in/out |
| Modal enter | 200ms | ease-out | slideUp animation |
| Loading pulse | 1200ms | ease-in-out infinite | Processing indicators |

**Rules:**
- Never exceed 250ms for interactive feedback
- Loading animations should be 1.2-1.5s cycles
- Always implement `prefers-reduced-motion: reduce`

---

### Icons

**Library:** Lucide React (stroke style, `stroke-width: 1.5-2`)

| Size | Usage |
|------|-------|
| 16px | Inline with text, badges |
| 18px | Navigation items |
| 20px | Button icons, card headers |
| 24px | Standalone, empty states |

No emoji in UI. No filled icon variants. Icons inherit `currentColor`.

---

### Component Patterns

#### Button
```
Primary:   bg: var(--brand), color: #fff, radius: 14px, padding: 10px 12px
Secondary: transparent bg, 1px border, radius: 10px, padding: 8px 24px
Small:     radius: 6px, padding: 5px 12px, font-size: 12px
Icon:      28×28px, radius: 6px
```

#### Input
```
Height: 36px, radius: 8px, border: 1px solid var(--border), padding: 4px 12px
Focus: border-color: var(--brand), box-shadow: 0 0 0 1px var(--bg-card), 0 0 8px var(--brand)
```

#### Card
```
bg: var(--bg-card), border: 1px solid var(--border), radius: 12px
padding: 12px, shadow: 0 1px 2px rgba(0,0,0,0.08)
```

#### Chat Bubble
```
bg: rgba(26, 28, 31, 0.92) with blur(16px)
border: 1px solid rgba(255, 255, 255, 0.08)
radius: 14px, speech-bubble triangle
User bubble accent: var(--role-[current-pet])
```

#### Tool Card
```
bg: var(--bg-card), border: 1px solid var(--border-subtle)
radius: 10px, collapsible, auto-collapse after 4s
```

#### Status Pill
```
radius: 999px, padding: 2px 8px, font-size: 11px, font-weight: 600
Colors: semantic background + semantic text
```

#### Navigation Item
```
radius: 10px, min-height: 38px, padding: 8px 10px
Active: bg var(--nav-active), left border accent, color var(--brand)
```

---

### Window Specifications

Clawd uses multiple Electron BrowserWindow types:

| Window | Size | Frame | Transparent | Notes |
|--------|------|-------|-------------|-------|
| Pet overlay | 160×160 | No | Yes | Always-on-top, click-through toggle |
| Chat panel | 400px wide | No | Yes | Slide-in sidebar, glass effect |
| Settings | 680×520 | Yes | No | Standard window, sidebar nav |
| Quick input | 320×48 | No | Yes | Floating above pet, glass effect |

---

### Layout

#### Settings Window
```
Grid: sidebar 160px | main 1fr
Padding: 28px content area
```

#### Chat Panel
```
Width: 400px, full height
Slide-in from right: 250ms ease-out
```

#### Pet Window
```
Pet GIF: 128×128px centered in 160×160 transparent frame
Chat bubble: absolute positioned above pet
```

---

### Accessibility

| Requirement | Implementation |
|-------------|---------------|
| Color contrast | WCAG AA minimum (4.5:1 for text, 3:1 for UI) |
| Focus visibility | 2-3px brand-colored ring on all interactive elements |
| Reduced motion | Kill all animation and blur via media query |
| Touch targets | Minimum 36px height on all interactive elements |
| Font scaling | Rem-based sizing where possible, min 11px |

---

### Implementation Strategy

1. **CSS Variables** — Create a `tokens.css` with all `--*` tokens above, import in renderer
2. **Replace hardcoded values** — Systematically replace inline hex/rgba with `var(--token)`
3. **Lucide React** — `npm install lucide-react`, replace any text/emoji icons
4. **Glass effect** — Apply `backdrop-filter: blur(16px)` to panels, chat, quick input
5. **Spacing grid** — Replace arbitrary values with `var(--space-*)` tokens
6. **Radius tokens** — Replace scattered values with `var(--radius-*)` tokens
